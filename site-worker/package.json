{
  "name": "site-worker",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "assemble": "node scripts/assemble-public.mjs",
    "check": "tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "wrangler": "^4.19.0"
  }
}
