---
title: "キーバインド"
description: "bmd のキー操作一覧"
sidebar:
  order: 3
---

既定のキーマップです。上書きは [設定](/projects/bmd/configuration/) の `[keymap.*]` で行います。

## Normalモード

| キー | 動作 |
|---|---|
| `j` / `↓` | 2行下へスクロール |
| `k` / `↑` | 2行上へスクロール |
| `d` / `PageDown` | 半ページ下 |
| `u` / `PageUp` | 半ページ上 |
| `g` / `G` | 先頭 / 末尾 |
| `[` / `]` | 前 / 次の見出し |
| `Tab` / `n` | 次の可視リンク（検索中は次ヒット） |
| `Shift-Tab` / `N` / `p` | 前の可視リンク（検索中は前ヒット） |
| `o` / `Enter` | 選択リンクを開く / プレビュー（`#anchor`は文書内ジャンプ） |
| `O` | プレビューを閉じる、またはナビ1段戻る |
| `/` / `?` | 前方 / 後方検索開始 |
| `h` / `H` | ヘルプ表示 / 閉じる |
| `x` | 画面上部のタスクリスト項目をトグル |
| マウスホイール | スクロール |
| `q` / `Ctrl-c` | 終了（`Esc`は検索クリア、またはアンカー/文書スタックのリセット） |
| リンクを左クリック | 開く / プレビュー |
| チェックボックスを左クリック | トグル（Normalのみ） |
| ドラッグ | テキスト選択（離すとクリップボードへ） |
| `y` | 現在の選択をコピー |

## 検索入力モード

| キー | 動作 |
|---|---|
| 文字 | クエリに追加 |
| `Backspace` | 1文字削除 |
| `Enter` | 確定してNormalへ |
| `Esc` | 入力キャンセル |

## Previewモード

| キー | 動作 |
|---|---|
| `Esc` / `o` | プレビューを閉じる |
| `+` / `=` / `-` | ズームイン / アウト |
| `0` | フィットにリセット |
| Ctrl+トラックパッドピンチ | ズーム |
| プレビュー外を左クリック | 閉じる |
| `q` / `Ctrl-c` | 終了 |

## リンクの種類

| 種類 | 例 | `o` / `Enter` |
|---|---|---|
| Web | `[text](https://…)` | OSのブラウザで開く |
| Anchor | `[text](#section)` | 見出しへジャンプ（履歴スタックあり） |
| Document | `[text](./other.md)` | 同ビューで開く（ファイルスタック） |
| Image | `![alt](path.png)` | フローティングプレビュー |
| Mermaid | mermaidコードブロック由来 | 描画プレビュー |

`O`はプレビュー閉鎖とナビ戻りの両方に使います。アンカーと文書ナビが両方あるときはアンカーが優先されます。各スタックは最大64層。

## 関連ページ

- [クイックスタート](/projects/bmd/quickstart/)
- [設定](/projects/bmd/configuration/)
