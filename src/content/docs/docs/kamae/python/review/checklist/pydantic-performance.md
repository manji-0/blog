---
title: "Pydantic パフォーマンス チェックリスト"
sidebar:
  order: 5
  label: "Pydantic パフォーマンス"
---

リファレンス: [`pydantic-performance.md`](/docs/kamae/python/references/pydantic-performance/)。

## 18.1 `model_construct` は信頼できる内部経路に限定されているか — High

完全バリデーションが必要な境界データ、ORM 行、部分的に信頼できる dict への `model_construct` を指摘する。

文書化された不変条件で既に検証済み内部状態を再水和するホットパスでの `model_construct` は指摘しない。

## 18.2 高頻度境界は意図的に最適化されているか — Low

必要箇所で完全バリデーションがまだ適用されている根拠なく、ホットエンドポイントで高コストなネスト検証を行う箇所を指摘する。

プロファイリングやスループット要件がトレードオフを正当化するときだけ msgspec や狭い DTO アダプターを提案する。

## 18.3 パフォーマンスショートカットは不変条件を保つか — High

余分フィールド、古いバージョン、無効な判別子をドメインコードに入れうる、スキップされたバリデータ、無効化された設定チェック、キャッシュアダプターを指摘する。
