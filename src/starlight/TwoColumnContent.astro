---
const { starlightRoute } = Astro.locals;
const showRightRail = starlightRoute.toc || starlightRoute.hasSidebar;
---

<div class="lg:sl-flex">
	{
		showRightRail && (
			<aside class="right-sidebar-container print:hidden">
				<div class="right-sidebar">
					<slot name="right-sidebar" />
				</div>
			</aside>
		)
	}
	<div class="main-pane"><slot /></div>
</div>

<style>
	@layer starlight.core {
		.main-pane {
			isolation: isolate;
		}

		@media (min-width: 72rem) {
			.right-sidebar-container {
				order: 2;
				position: relative;
				width: calc(
					var(--sl-sidebar-width) + (100% - var(--sl-content-width) - var(--sl-sidebar-width)) / 2
				);
			}

			.right-sidebar {
				position: fixed;
				top: 0;
				border-inline-start: 1px solid var(--sl-color-hairline);
				padding-top: var(--sl-nav-height);
				width: 100%;
				height: 100vh;
				overflow-y: auto;
				scrollbar-width: none;
			}

			.main-pane {
				width: 100%;
			}

			:global([data-has-sidebar]) .main-pane {
				--sl-content-margin-inline: auto;

				order: 1;
				width: calc(
					var(--sl-content-width) + (100% - var(--sl-content-width) - var(--sl-sidebar-width)) / 2
				);
			}

			:global([data-has-sidebar]) .right-sidebar-container {
				width: var(--sl-sidebar-width);
			}

			:global([data-has-sidebar]) .main-pane {
				width: calc(100% - var(--sl-sidebar-width));
			}
		}
	}
</style>
